uniform sampler2D texture;
uniform vec2 mouse;
vec2 lensRadius = vec2(150, 60);

void main (void)
{
	vec4 color = texture2D(texture, gl_TexCoord[0]);
	float distance = distance(mouse, gl_FragCoord);
	color.a = smoothstep(lensRadius.x, lensRadius.y, distance);
	gl_FragColor = color;
}